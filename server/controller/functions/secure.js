// const bcrypt = require('bcrypt');
// const saltRounds = 12;

// // export const genSalt = async () => {
// //   return bcrypt.genSalt(saltRounds);
// // };

// // export const hash = async (plainPassword, salt) => {
// //   return bcrypt.hash(plainPassword, salt);
// // };

// module.exports = {
//   genSalt: async () => {
//     return bcrypt.genSalt(saltRounds);
//   },
//   hash: async (plainPassword, salt) => {
//     return bcrypt.hash(plainPassword, salt);
//   },
// };
